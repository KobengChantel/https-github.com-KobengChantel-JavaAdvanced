package Lesson6;

// Simple utility class with a method to check if a string contains a substring.

public class AnalyzerTool {

  // Returns true if sourceStr contains searchStr
  public boolean arrContains(String sourceStr, String searchStr){
    return sourceStr.contains(searchStr);
  }
}
