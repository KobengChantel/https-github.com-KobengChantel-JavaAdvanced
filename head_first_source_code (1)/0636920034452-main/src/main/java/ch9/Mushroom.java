package ch9;

/**
 * The Mushroom class represents a mushroom object with attributes such as size and whether it is magical.
 * It provides multiple constructors to create Mushroom instances with different parameter combinations.
 */
public class Mushroom {

  /**
   * Constructs a Mushroom with a specified size.
   *
   * @param size the size of the mushroom
   */
  public Mushroom(int size) {
    // Constructor logic to initialize size (if implemented)
  }

  /**
   * Constructs a Mushroom with default properties.
   */
  public Mushroom() {
    // Default constructor logic (if implemented)
  }

  /**
   * Constructs a Mushroom indicating whether it is magical.
   *
   * @param isMagic true if the mushroom is magical, false otherwise
   */
  public Mushroom(boolean isMagic) {
    // Constructor logic to initialize magical property (if implemented)
  }

  /**
   * Constructs a Mushroom with specified magical status and size.
   *
   * @param isMagic true if the mushroom is magical, false otherwise
   * @param size the size of the mushroom
   */
  public Mushroom(boolean isMagic, int size) {
    // Constructor logic to initialize both properties (if implemented)
  }

  /**
   * Constructs a Mushroom with specified size and magical status.
   * This constructor differs in parameter order from the previous one.
   *
   * @param size the size of the mushroom
   * @param isMagic true if the mushroom is magical, false otherwise
   */
  public Mushroom(int size, boolean isMagic) {
    // Constructor logic to initialize both properties (if implemented)
  }
}
