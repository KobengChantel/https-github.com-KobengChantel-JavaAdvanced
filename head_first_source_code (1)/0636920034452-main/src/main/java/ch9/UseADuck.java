package ch9;

/**
 * The UseADuck class demonstrates creating a Duck object
 * by calling its constructor with an integer parameter.
 */
public class UseADuck {

  /**
   * The main method is the program entry point.
   * It creates a new Duck object with a specified size.
   */
  public static void main(String[] args) {
    // Create a new Duck object with size 42
    Duck d = new Duck(42);
  }
}
